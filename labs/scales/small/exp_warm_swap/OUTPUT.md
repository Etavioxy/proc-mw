# 实验 · 预热换入（canary 模式）· 输出

> 运行：`cd labs/scales/small && cargo run --release --bin exp_warm_swap`

## 运行输出（2026-08-07）

```
[1] v1 就绪，开始服务
[2] 准备 v2 期间 v1 服务 8290936 条（后台准备，服务不停；准备耗时 241.908208ms）
[3] swap（chain.set）耗时：500ns（用户可见重载 = swap，非编译+加载）
实验通过：预热换入 — swap ~µs，660ms 准备在后台，服务不停 ✓
```

## 证据解读

| 证据 | 含义 | 维度 |
|---|---|---|
| v2 编译+加载（242ms）在后台，v1 服务 8.3M 条不停 | **准备与提交分离**（canary 模式） | D3/D6 |
| **swap（chain.set）500ns** | 用户可见重载 = 500ns，非 ~660ms 编译+dlopen | D4/D6 |
| 上轮 dlopen 发现（~500ms/新 .dylib）→ 本实验解法 | dlopen 是**准备成本**，可后台化 | D5 |
| v2 生效（[v2] 标签） | 换入后新逻辑立即生效 | D3 |
