# 实验 · 字节沙箱 + 直接依赖宿主插件 · 输出

> 运行：`cd labs/scales/large && cargo run --release --bin exp_sandbox_hostdep`

## 运行输出（2026-08-07）

```
[1] 沙箱内（依赖宿主的插件）处理：SandboxMsg{a:7,b:10} → b=11（期望 11）
实验通过：字节沙箱 + 直接依赖宿主插件（集成缺口闭合）✓
```

## 证据解读

| 证据 | 含义 | 维度 |
|---|---|---|
| 依赖宿主的插件在子进程内变换 repr(C) 类型 | 字节沙箱 + 直接依赖宿主（真实世界插件形态）集成 | D7/D6 |
| SandboxMsg repr(C)/POD 跨进程字节编组 | 沙箱适用 POD；堆类型仍显式边界 | D7 |
| **发现：mw_exec 二进制是旧构建（无 --bytes）会挂起** | **部署注意**：沙箱执行器协议变化需重建——工具链分发/版本域 | D5/D8 |
