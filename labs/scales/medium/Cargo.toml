[package]
name = "medium_service"
version = "0.1.0"
edition = "2021"
description = "三档体量测试·中型：生成 ~万 LOC 多模块服务用 proc-mw 方法"

[dependencies]
proc-mw = { path = "../../..", features = ["runtime"] }
futures = "0.3"
flume = "0.11"
# medium（tower 语义）场景：真实 tower Service 集成 + 直接共享类型
tower = { version = "0.5", features = ["util"] }
shared_types = { path = "../../../labs/shared_types" }
