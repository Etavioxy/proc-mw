# 八维核心约束 · 凝练版

> 2026-08-06 会话逐维确认后的定稿。细节与纠错轨迹见 `DESIGN-DRAFT.md` §6。
> 目标系统：**生产 Service 语义（tower）× 运行期热重载（evcxr 机制）** 的中间件层，植入大型 Rust 系统。

---

## 1 · 表达层 — 零成本抽象

业务代码零污染（核心不知道被包裹）；装配表达式编译后与手写裸调用**机器码级等价**。弱化形式 = Release 零成本：Debug 全动态，Release 下 cfg 在词法层剥离，退化为裸调用。

## 2 · 类型通道 — 零成本分发

分派机制按**状态承载与类型开放性**独立可选、可混合：enum（封闭/有状态内联）、`fn` 指针（无状态/thin）、位标记（开关）、`dyn`（开放/有状态）。每个中间件**只付实际需要的成本**。通道语义（短路/上下文/错误/async）与分派机制**正交**。

## 3 · 动态性 — 增删在数据/快照层

链是不可变快照（RCU），`add/remove` = 替换快照；读路径**无锁、无分配、零成本**。运行时承诺的是"实例增删"，不是"类型新增"。

## 4 · 性能 — 局部加法 + 空链透明

动态加中间件 = 每链每请求 **+Θ(落槽代价)**，局部加法，**不影响全局**（无全局锁/共享可变状态/缓存污染）。空链路径（**含 Debug**）与裸调用基本无异：不分配、不加锁、无可测指令。理论代价 = 实测代价。

## 5 · 编译层 — 研究对象是 LLVM 结构 + 真实动态链接

分派决策在 LLVM 层的真实形态被验证：enum 必去虚拟化、dyn 仅 ThinLTO+PGO 下才可能去虚拟化、cfg 不进 IR、增量失效按 **CGU 粒度**。动态链接的不透明边界（符号插值、重定位、无跨边界优化）代价**被显式接受并测量**。

## 6 · 扩展形态 — 运行期动态编译 + 加载（evcxr 参考，非照搬）

中间件集合增长的默认方式：写代码 → 运行时编译 .so → dlopen → 查符号 → 注册，支持不停机热替换。**它是 D1~D5 的超集，不推翻它们**：加载产物按 D2 落槽（无状态→thin fn-ptr，有状态→dyn），不强制开放化。**拿 evcxr 的管线**（syn 解析→crate 编译→.so→dlopen→符号→缓存→永不卸载），**丢 REPL 壳**（子进程、变量持久化、逐求值重编、输出捕获）。

## 7 · 安全与定位 — 不安全收口边界层 + 信任模型明确

ABI 契约用 `extern "C"` + 版本符号显式化；dlopen/符号解析/指针转换收进薄 boundary；panic 跨 FFI 用 `catch_unwind` 兜住；加载的中间件状态必须 Send+Sync；热替换**永不直接 unload**（防 TLS destructor）。**定位 = tower（生产语义）× evcxr（运行期加载）的生态交集**——生态里没有同时满足两者的库。

## 8 · 迁移工具链（最后做）— 渐进、按域、语义等价

静态分析识别候选核心与**内联横切逻辑** → AST 改写（codemod）抽取中间件 + 包装核心 → 按域渐进采纳（加性安全、可回滚）→ 编译反馈闭环（快速产出可编译骨架）。目标是让 N 个已有 handler 一次性、可回滚地搬上中间件层。

---

## 维度间关系

- **D6 前置约束 D2**：运行期扩展要求分派支持开放世界（fn-ptr/dyn 槽位），闭合 enum 无法被运行期扩展
- **D3 锁死 D4**：空链透明（含 Debug）要求读路径无锁无分配 → RCU 快照而非 Mutex
- **D5 是 D6 的载体**：evcxr 的编译→dlopen→重定位正是 D5 要研究的真实动态链接
- **D1~D5 是纸面承诺，D4 是验收**：理论代价必须等于实测代价（基准 + 反汇编）
