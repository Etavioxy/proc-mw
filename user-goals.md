# user-goals.md — proc-mw 中间件系统

> 用户持续变化的约束。每改动必须同步本文件，再输出方案。设计八维定稿见 `CORE-CONSTRAINTS.md`。

## 工作方法约束

- **多维门禁**：只有确实满足所有约束条件（八维交叉验证到极致）才可以删除 cron；必须多重测试。
- **维度是交叉检查不是关卡**：八维约束施加在同一 proc-mw 系统上，不是依次通关的流水线任务。
- **单一项目**：不是一个维度一个项目；所有维度收敛进 proc-mw 单一 crate（labs/ 为测量台）。
- **测试场景是子任务**：每个必要考虑的维度配一个测试场景，检测代码形式的效果；但不影响对中间层本体的持续探索，测试是辅助不是主线。
- **走上正路的判据**：直到发现每个维度都有"做不到极致"的情形、并反复修改比对时，才说明走上正路。要持续思考——主动寻找每维约束的极限与失败情形，反复迭代"设计 ↔ 约束"，直至系统对其局限诚实，而不是逐维确认"通过"。
- **全面研究分派与值空间（不限于少数机制）**：D2 的类型通道分派机制不止已实现的少数几种——一般结构体、容器、一般值对象都要研究；即使明显不行的也要研究，且必须严谨（每项要有证据与判定，含不可行的原因）。"追求极致就是要全部都要了解"，不要局限。
- **不强制 dyn**：运行期加载的产物不一定要是 dyn——按中间件特性（无状态→thin、有状态→dyn 等）落最合适的槽位，dyn 只是分派机制之一。
- **验证第 2 维各种情况**：D2 的每种情况都要验证，dyn 只是其中一种；穷举设计空间并逐项研究。
- **持续反思未考虑边界**：远未达到 cron 满足水平；要主动反思"自己还有什么边界没考虑"，追求极致。不许宣布维度"可收口"——每个维度都有未推的边，持续探索。
- **核心目的 = 编译任意 Rust 代码粘合进中间层**（evcxr 机制）：这是整个系统存在的意义。不是固定 ABI 的 i32 插件——是能在运行期编译**任意 Rust 代码**（操作任意类型的方法，如 `String::push`/`Vec::sort`/struct 字段）并加载进中间件链。当前 i32 插件 ABI 只是玩具，方向是类型无关 ABI（`*mut c_void`）+ 共享类型定义 + 运行期编译管线。
