# Rust 中间件系统（Proc / DynamicProc）设计探讨 · 过程草稿

> 状态：**草稿**（DRAFT）—— 全面记录 2026-08-06 的完整探讨过程，含结论与纠错轨迹，供后续设计与实验引用。
> 性质：过程记录，非定稿设计文档。
> 更新：维度框架已从 7 维扩展为 **8 维**，逐维核心约束定稿见 §6（细节）与 `CORE-CONSTRAINTS.md`（凝练版）。

---

## 0. 背景与目标

- **核心想法**：在 Rust 里做一个"Proc 中间件"系统——业务核心（纯函数）与横切逻辑（中间件）解耦，中间件在核心进入/退出时插入逻辑，且支持运行时动态增删。
- **弱化约束**：追求 **Release 零成本**——Debug 下全功能动态，Release 下剥离所有中间层，退化为裸函数调用。
- **终极约束（本会话确立）**：该系统最终要**植入大型 Rust 系统**。一切设计与指标取舍必须以此为判据。

---

## 1. 原始设计演进（前序对话摘要）

### 1.1 Proc 中间层 → 语义极简化

- 用 **trait 协议** 定义 `Proc { fn exec(&self, x: i32) -> i32 }`。
- 纯业务模块（如 `Add`）零污染，不含任何 cfg / println。
- 中间层（如 `Log<T>`）独立于业务之外，泛型包装。
- **挂载语义**：用**变量遮蔽** `let p = Log(p)` + `#[cfg(debug_assertions)]` 表达"只在 Debug 下套壳"，Release 下该行在词法阶段被删除，`p` 类型直接是 `Add`。

### 1.2 DynamicProc —— 运行时动态中间件

- `Middleware { on_enter(&mut i32), on_exit(&mut i32) }`（洋葱模型：进入正序、退出逆序）。
- `FlexMiddleware` 持有 `Box<dyn Fn(&mut i32)>`，运行时传任意闭包 → 可配置任意逻辑。
- `DynamicProc { core: Box<dyn Fn(i32)->i32>, middlewares: Vec<Box<dyn Middleware>> }`，提供 `add/remove/clear/exec`。
- 代价：每次 exec 遍历 Vec、动态分发、堆内存；换来运行时的灵活性。

### 1.3 Release 零成本（弱化形式）

- `DynamicProc<C> { core: C, #[cfg(debug_assertions)] middlewares: Vec<Box<dyn Middleware>> }`。
- Release 下 `middlewares` 字段不存在，`add/remove` 编译为无操作，`exec` 直接 `(self.core)(input)`，泛型单态化 + 内联 → 裸调用。
- cfg 在**词法解析阶段**删码，比 `if false` 更彻底，Release 编译还更快。

### 1.4 与传统 AOP 的对比（4 个根本变化）

| 维度 | 传统 AOP（Java/Spring） | 本方案 |
|---|---|---|
| 目的 | 架构级横切约束（事务/权限，生产刚需） | 编译级可剥离调试/观测层 |
| 成本模型 | 运行时反射/代理，无法归零 | cfg 完全归零（Release 下不存在） |
| 动态边界 | 运行时可控（JMX 等） | 编译期固化（Release 无法加回） |
| 类型系统 | 反射擦除、字符串切点、编译期不安全 | 泛型构造、编译器报错、类型安全 |

- 结论：准确名称是"**编译期装饰器 + 条件编译**"，保留了 AOP"业务与横切解耦"的灵魂，失去了切点表达式与运行时横切能力。
- 定位：**手术台的灯（Debug 照亮，Release 关掉）**，不是系统的骨架。

### 1.5 大型代码库编译代价（前序估算）

- 50 万行规模：全局切面 10~30 个，局部 50~200 个，被包裹业务函数 1000~5000。
- 单态化实例 ≈ 2000~15000 个；Debug 构建从 2 分钟膨胀到 8~12 分钟，Release 到 15~25 分钟。
- **致命点：增量编译爆炸**——改一个泛型切面，依赖它的所有实例失效重编，锯齿状重编译风暴。
- 建议：泛型中间件限定在边缘入口层（<50 个函数），核心领域保持裸函数。

### 1.6 JIT / 动态 DLL 扩展

- **JIT**（cranelift/inkwell）：笨重（打包整个编译器后端），仅适合用户自定义规则引擎，杀鸡用牛刀。
- **DLL**（libloading）：每个中间件独立编译成 .so/.dll，修改只重编该库（秒级），用 `extern "C"` 函数指针规避 Rust 不稳定 ABI，函数指针调用 ≈1~2ns。
- 三者对决：编译期泛型（灾难/极致/无热更新）vs JIT（极慢启动/极高/支持）vs DLL（极佳/优秀/支持）。
- DLL 让 DynamicProc 从"调试工具"升级为"生产级插件化 AOP 容器"，支持不停机更新。

### 1.7 生态现状（相关度 × Star 排序）

- **第一梯队（理念最接近）**：dymod、hotload(627★)、libhotpatch(94★)。
- **第二梯队（插件系统）**：fidius(48★)、kokoro(33★)、relib(28★)、fnaop(0★)、aspect-core/aspect-runtime。
- **底层**：libloading(1.2k★)——生态事实标准，但不提供任何 AOP/热重载逻辑。
- **为什么没有"终极库"**：零成本文化、"不用则零开销"哲学、Rust 不稳定 ABI 是天堑、社区偏好"可组合零件"而非"一体化成品"。

---

## 2. 本会话探讨历程（维度 → 指标 → 核心）

### 2.1 七维问题框架（最初提出的维度）

1. **表达层**：中间件"长什么样"——trait 形态、挂载语法（遮蔽 vs 注册 vs 宏）、洋葱顺序、能否短路。
2. **类型通道**：中间件之间传什么——`&mut Context / Result<T,E>` 还是 `i32`；链与核心签名解耦还是同构；输入能否被替换；错误如何传播。**← 决定通用性上限。**
3. **动态性**：增删机制（索引 vs ID vs 优先级）、核心函数是否可换、线程模型（Sync）、Drop 语义。
4. **性能**：完整零成本 vs Release 零成本（已选后者）；`Box<dyn>` 的 vtable 是否还残留。
5. **编译层**：单态化爆炸、增量风暴、cfg 裁剪 vs 泛型的类型检查成本。
6. **扩展形态**：源码内联 / proc-macro / DLL / JIT 的取舍表。
7. **安全与定位**：DLL 边界的 unsafe、ABI、Send/Sync；与生态现有库的差异化定位。

### 2.2 指标聚焦——迭代与纠错轨迹

> 用户连续四次纠正，逐步把方向从"编译/机器码"掰回"生产/语义"。

| 轮次 | 提出的指标 | 判定 | 原因 |
|---|---|---|---|
| ① | 增量重编译波及面（改一个中间件重编译多少） | **否** | 是症状，不是根因 |
| ② | 编译正交性（中间件×核心是 N×M 还是 N+M） | **否** | 仍停留在编译层，且测了已被抛弃的泛型中间件形态（稻草人） |
| ③ | 消除层级 / 零抽象（源码层 cfg vs 编译层内联 vs 运行时 dyn） | **否** | 仍是机器码/代码生成层视角 |
| ④ | **prod 与核心：生产中间件必须存在** | **是** | 见 2.3 |

### 2.3 prod 是什么 + 核心是什么（转折点）

- **prod = 中间件必须真实存在的地方**。cfg 剥离意味着 Release 里这些中间件不存在 → 此设计里的中间件本质是**开发/观测工具**。
- 但生产真正需要的是**必须常驻、绝不能剥**的中间件：鉴权、事务、限流、链路追踪、校验。对它们：零成本抽象不适用（要干真实工作）、cfg 剥离不可能（剥了系统就洞开）。
- **核心 = trait 的形状 + 并发安全**。生产中间件不是 `on_enter(&mut i32)` 的"观察-放行"模型，需要：
  - **短路**（鉴权失败直接 return，不碰核心）
  - **上下文传递**（一个 Request 贯穿整条链）
  - **错误传播**（返回 Result，失败即中断）
  - **Send + Sync**（链被 Arc 共享，无数请求并发走同一条链）
  - **async**（中间件可能做 IO）
  - **增删与执行并发**（add/remove 与飞行中的 exec 竞争）
  - 这正是 **Tower `Service` 形状**：`call(&self, req) -> impl Future<Output=Result<Resp, Err>>`，中间件 = 包装 + `next()`。
- 推论：生产中间件的问题域（async、并发安全、短路、错误语义）是 tower 已占领的领域；"Debug 动态 / Release 消失"只是开发技巧，两者不是同一个物种。

### 2.4 数量与代码量的关系（标度律）

- **不同的中间件类型数 D**：全局级 10~20 个（与代码量无关）+ 领域级每领域 2~4 个 → **随领域数增长，次线性、饱和**。300k 行 ≈ D≈100；1M 行 ≈ D≈150~300。加新功能很少发明新中间件，都是复用。
- **挂载点数 N**：随处理器数**线性**增长（每 handler 一个 DynamicProc = 一条 3~8 层链）。300k 行后端 ≈ 1000~3000 挂载点。
- **真正随代码量线性膨胀的是胶水代码**：按每 handler 5 行装配 = 2000×5 = 1 万行 ≈ 3% 代码；15 行 → 3 万行 ≈ 10%，开始反噬。
- 设计结论：**中间件层目标 = 压成 <1% 的常数**。核心功能不是性能/编译技巧，而是把每处挂载胶水压到 0~1 行（默认链模板 scenario + 单行装配宏/配置化）。
- 顺带量化了泛型中间件必败：D×N（100×3000=30 万）把"饱和的类型层"和"线性的装配层"相乘，重新线性爆炸。

### 2.5 复杂度数学分析（确立）

设 **L**=总代码量，**N**=处理器数，**D**=不同中间件类型数，**C**=链长。

**标度律**：N=Θ(L)；C=Θ(1)（有界 3~8）；D=O(L^β)，β<1（次线性饱和）。

**乘积守恒**：系统里中间件信息量 = N 挂载点 × C 槽位 × log D bits = **Θ(N·C·log D)**，无法消灭，只能选择物化位置：

| 物化位置 | 代价 | 量级 |
|---|---|---|
| 类型系统（泛型嵌套） | 编译期：每链是唯一类型，C 层各单态化 | 干净构建 Θ(N·C)=Θ(L)，**常数 ×C** |
| 运行时（dyn+配置表） | 运行期：每链存 C 个指针 | 内存 Θ(N·C)=Θ(L)，每请求 Θ(C)=Θ(1) |
| 源码（胶水代码） | 每挂载点 g 行 | Θ(N·g)=Θ(L) 行 |

理论类型空间 D^C = L^(β·C)（指数级），但配置以类型表达时实际只实现 Θ(N) 个 → 泛型不爆指数，爆在常数与增量。

**复杂度全表**：

| 代价 | 泛型（类型物化） | dyn（运行时物化） |
|---|---|---|
| 干净构建 | Θ(L)×C（每实例过 LLVM） | Θ(N+D)=Θ(L)，小常数 |
| 改一个中间件增量重编 | **Θ(f·N)=Θ(L)**（f≈1 全局中间件） | **Θ(1)** |
| 改一个 handler 增量 | Θ(1) | Θ(1) |
| 二进制体积 | Θ(N·C)=Θ(L) 每实例展开 | Θ(D) 共享 + Θ(N) 薄壳 |
| 每请求运行时 | Θ(1)（全内联） | Θ(C)=Θ(1)（有界间接调用） |
| 启动内存 | 0 | Θ(N·C)=Θ(L) 次分配 |

**决定性的差异在增量项**：泛型重构期改 K 次中间件 = Θ(K·f·N)=Θ(K·L) 超线性；dyn = Θ(K) 编辑局部性完整保留。

**数学落回设计**：
- 配置必须是**数据**，不能是类型（链 = handler→中间件 ID 查表）。
- 中间件 = 共享代码（dyn，Θ(D) 有界）。
- 装配 = Θ(1)。
- 结论：整体 Θ(L) 无法回避（N 线性不可削减），但常数因子、增量指数、乘积物化位置三个自由度，数学上只有一个占优解——**dyn 分发 + 数据配置 + 有界 D**。即使 β=1 最坏情况结论不变。

---

## 3. 核心指标（最终聚焦）

### 3.1 回到七维：哪些被解决/降级/未解剖

| 维度 | 状态 |
|---|---|
| #4 性能/零成本 | 已解决：prod 中间件必须存在，零成本不适用 |
| #5 编译层 | 已降级：只是"乘积物化位置"的后果 |
| #3 动态性 | 已降级：是乘积的来源（信息量），不是度量 |
| #6 扩展形态 | 已被复杂度分析覆盖：dyn 占优 |
| #7 安全与定位 | 生产刚需，但不是指标 |
| #1 表达层 | ≈ 胶水代码，线性项，可优化到 Θ(1) |
| **#2 类型通道** | **唯一未被解剖、却决定其他一切的维度** |

### 3.2 为什么类型通道是核心维度

它是所有其他维度的**上游**：

| 由类型通道决定 | 对应维度 |
|---|---|
| object-safety → 能否 dyn → 成本模型 Θ(N+D) vs Θ(N×C) | #5 #6 |
| 能否表达短路/上下文/错误/异步 → 是否生产可用 | #3 #7 |
| Send/Sync → 并发安全 | #7 |
| 通用性上限 → 真实 D 规模、C 增长方式 | 标度律 |

通道窄（`i32→i32`）一切免谈：无上下文、不能短路、不能带错、不能 async，D 只能是 0；通道宽（context+error+short-circuit+async）生产形状成立，但代价与 dyn-ability 需重算。

### 3.3 核心指标定义

> **类型通道的"表达力 ÷ 代价"**，可测形式：
> **真实生产中间件覆盖度 ÷ 每次请求的链上附加开销**
> 约束：dyn 可对象化 + Send/Sync + 增量编译 Θ(1)。

### 3.4 五步剖析法

1. **锁定通道假说**：定义生产形状 trait（`call(&self, req, ctx, next) -> Result<Resp, Err>` + async），穷举它能表达/不能表达的语义集合（能否终止链？能否在中间件内部换 ctx？错误能否被上游吞掉重试？）。
2. **覆盖度测试**：真实中间件清单（鉴权、限流、追踪、校验、重试、熔断、超时）逐个实现，统计能塞进 dyn 通道的比例 + 每个失败点根因——直接测"通用性上限"，即真实 D 能到多少。
3. **代价测试**：恒载 5 链并发压测，量每请求附加 ns 与 p99 退化，对比手写展开——"代价"分母。
4. **编译/增量验证**：N×C×D 合成工程，验证干净构建 Θ(N+D)、改中间件增量 Θ(1)。
5. **合成判据**：核心分数 = 覆盖度 / 每请求 ns，要求 95%+ 覆盖、<50ns 附加、增量 Θ(1)。

> 第 1、2 步是灵魂——覆盖度是唯一没被量化过的项，其余（代价、编译）已模型化。

---

## 4. 待验证的开放问题（实验清单）

- [ ] **通道形状**：定义生产形状 trait，穷举能/不能表达的语义（第 1 步）。
- [ ] **覆盖度**：7 类真实生产中间件逐一验证 dyn 可表达性（第 2 步）。
- [ ] **代价**：恒载链并发压测，测每请求附加 ns / p99（第 3 步）。
- [ ] **编译/增量**：N×C×D 合成工程验证 Θ 模型（第 4 步）。
- [ ] **Release 零成本复核**：确认 dyn + cfg 方案下 Release 干净构建的常数因子。
- [ ] **胶水比例**：量化默认链模板 + 配置化后，中间件层代码占全工程比例 <1% 是否成立。

---

## 5. 已确立的结论（供引用）

1. **中间件分两类**：类型层（饱和、有界 Θ(L^β)）+ 装配层（线性 Θ(L)）；线性膨胀的是胶水代码。
2. **乘积守恒**：信息量 Θ(N·C·log D) 不可消灭，只能选物化位置（类型/运行时/源码）。
3. **渐进结论**：整体 Θ(L) 不可回避；分胜负的是常数因子、增量指数、物化位置。
4. **唯一占优解**：dyn 分发 + 数据配置 + 有界 D + Θ(1) 装配。
5. **核心维度**：类型通道——决定 object-safety、生产形状、并发安全、通用性上限。
6. **核心指标**：覆盖度 ÷ 每请求附加开销，约束 dyn 对象化 + Send/Sync + 增量 Θ(1)。
7. **定位**：生产中间件问题域是 tower 的领域；本系统的差异化 = 生产可用 Service 层 + 开发时增强观测开关。

---

## 6. 八维核心约束定稿（2026-08-06 会话二，逐维确认）

> 本会话将 7 维框架扩展为 8 维，逐维敲定**核心约束**，含 4 次纠错轨迹。
> 凝练版见 `CORE-CONSTRAINTS.md`；本节记录完整推理与迭代过程。

### 6.0 过程概要

用户要求"每次说一个维度的核心约束，让我确认"。逐维推进中发生的关键纠错：

| 维度 | 首次作答（错） | 纠错方向 | 最终约束 |
|---|---|---|---|
| D1 表达层 | 胶水代码 Θ(1)（复杂度） | **和复杂度无关，是零成本抽象** | 零成本抽象 |
| D2 类型通道 | 必须 object-safe（胖指针） | **一定要胖指针吗？** → 展开分发空间 | 零成本分发（异构落槽） |
| D3 动态性 | — | 直接确认 | 数据/快照层增删 |
| D4 性能 | Release 机器码等价 | 补两条：**局部加法 + 空链透明（含 Debug）** | 局部加法 + 空链透明 |
| D5 编译层 | 增量 Θ(1)（指标） | **应该研究 LLVM 结构 + 真实动态链接** | LLVM 形态验证 + 链接成本 |
| D6 扩展形态 | 编译期默认（源码/宏） | **默认形态就是运行期（evcxr）** | 运行期动态编译加载 |
| D6 二次 | evcxr 照搬，推翻 D1~D5 | **不能推翻原来的 5 条** | evcxr 参考，是 D1~D5 的超集 |
| D7 安全与定位 | — | 直接确认 | 不安全收口 + 信任模型 + 生态交集 |
| D8 迁移工具链 | 用户补充 | **最后才做** | 渐进、按域、语义等价 |

### 6.1 维度 1 · 表达层 — 零成本抽象

- 初次错误：把约束答成"装配胶水 Θ(1)/挂载点"（复杂度视角）。
- 纠正：**和复杂度无关，是零成本抽象**。即业务代码零污染（核心完全不知道被包裹），装配表达式编译后与手写裸调用**机器码级等价**。弱化形式 = Release 零成本：Debug 全功能动态，Release 下 cfg 在词法层撕掉整个抽象，退化为裸函数调用——无 vtable、无 Box、无痕迹。
- 这是整个设计从起点就在追的东西（遮蔽 + cfg 的由来）；胶水代码只是它随代码量增长的附带问题，不是约束本身。

### 6.2 维度 2 · 类型通道 — 零成本分发

- 初次作答：核心约束 = "trait 必须 object-safe（可 dyn 对象化）"。
- 追问：**"一定要胖指针吗？"** 由此展开零成本分发的完整设计空间：
  - **轴 A · 类型开放性**：闭合（enum+match 跳转表，无指针、加类型重编 Θ(N)）/ 半开放（索引注册表，ID→fn 指针表）/ 开放（dyn/DLL，胖指针、加类型不重编宿主）。
  - **轴 B · 状态承载位置**：内联（enum 变体）/ 间接（状态指针）/ 全局（thread-local / static）/ ZST（零内存）。
  - **轴 C · 分派粒度**：每节点分派 / **整链预编译**（标准链宏编译成单一函数，per-handler = 一次查表 + 一次直调，生产最可能赢家）/ 组合子嵌套（类型级，回到泛型乘积，否决）。
  - **轴 D · 链的存储与并发**：Vec / 固定数组 / SmallVec / RCU-Arc 快照（无锁读）。
  - **关键洞察**：零成本 = 按特性落槽，不是选一个万能机制——开关型→位标记（一次 bit test）、无状态→fn 指针（8B）、有状态且封闭→enum 内联、有状态且开放→dyn/DLL（对开放性付费）。可**异构混合**于同一条链。
- 最终约束：**分派机制必须按中间件的状态承载与类型开放性独立可选、可混合——每个中间件只付它实际需要的成本。通道语义（短路/上下文/错误/async）是正交的：它约束"调用方传什么"，不约束"分发怎么实现"。**

### 6.3 维度 3 · 动态性 — 增删在数据/快照层

- **增删 = 替换快照，不是原地修改**：链是不可变快照（Arc 持有），add/remove = clone 一份、改动、原子交换。读路径拿当前快照直接遍历，**无锁、无拷贝、零附加**——分发机制不被可变性污染。
- **写入代价有界且并发安全**：每次增删 Θ(C)（复制链长，常数），稀有操作；与飞行中 exec 天然隔离（RCU）。
- **"实例增删"≠"类型新增"**：动态性承诺"链里这个中间件加上/去掉"（数据层）；"运行时冒出新类型"是开放世界的独立决策（D6），付相应代价。
- 一句话：**动态性是配置级的（改数据），不是机制级的（改分派）。**

### 6.4 维度 4 · 性能 — 局部加法 + 空链透明

- 初次作答：抽象代价 ≤ 等价手写实现（严格零成本定义），无中间件路径 Release 机器码级等价。
- 用户补充两条，成为定稿关键：
  1. **局部加法性**：动态挂中间件 = 每链每请求 +Θ(落槽代价)。加在一条链上只影响那条链那次请求——不允许通过全局锁、共享可变状态、缓存污染把局部改动**放大成全局开销**。全局吞吐对任何单条链的增删不敏感。
  2. **空链透明（含 Debug）**：即使不挂任何中间件，抽象在**非 Release 下也与裸调用基本无异**——空链路径不分配、不加锁、不引入可测指令。Debug 下"框架存在"≠热路径被拖慢，保证开发态性能数字可信。
- 设计推论：第 2 条反过来**锁死 D3 的选择**——读路径必须无锁无分配（RCU 快照，而非 Mutex），否则空链透明在 Debug 下立刻破功。
- 验收：理论代价必须等于实测代价（基准比值 ≈1.0 + 反汇编逐字节比对）。

### 6.5 维度 5 · 编译层 — 研究对象是 LLVM 结构 + 真实动态链接

- 初次作答：增量失效 Θ(1)（指标）。
- 纠正：**"编译层？？？？不是应该研究 llvm 结构吗？或者研究真实的动态链接？"** 即：不是给指标，而是研究生成物在 LLVM 和链接器里的真实形态。
- **LLVM 结构层要研究**：
  - 单态化 → LLVM IR → **CGU 划分**：泛型实例怎么落进 codegen unit（默认 16 并行），改一个泛型按 **CGU 粒度**失效——"增量 Θ(1)"是机制验证后的推论，不是前提；实例集中在单 CGU 会退化成串行重编。
  - **enum vs dyn 在 LLVM 的真实形态**：enum → switch → 跳转表/比较链，**LLVM 必去虚拟化**，分支可预测；dyn → load vtable + indirectbr，仅 **ThinLTO + PGO 全程序信息**下才有机会去虚拟化。
  - **cfg 撕码的 LLVM 意义**：代码进不了 IR——不是"被优化掉"，是根本不在优化空间里（也是 Release 编译更快的原因）。
  - **LTO/ThinLTO 决定"透明壳"是否真透明**：核心与调用方跨 crate 时，wrapper 要内联成裸调用必须靠 ThinLTO 跨 crate 内联；LTO 关掉，跨边界就是真实调用开销。
  - **icache**：单态化爆炸不只拖慢编译，代码膨胀直接压指令缓存，**运行时也变慢**——编译与运行时共享"代码量"这个变量。
- **真实动态链接层要研究**：
  - dlopen 真实成本：符号解析 + 重定位。`extern "C"` fn 指针直调 ≈ 一次间接调用，无 PLT 开销；但**符号插值（preemption）**使编译器不能假设实现是最终的 → **跨动态边界优化全部关闭**（无内联、无常量传播、无去虚拟化）。
  - **不透明边界 = "开放性"的真实报价**。
  - 静态 vs 动态：静态+LTO 全程序可见最优；动态省内存/磁盘、支持热换，付插值 + 不可优化 + 加载重定位的税；启动时间与重定位量成正比。
  - PLT 与懒绑定：跨库普通函数走 PLT（首次解析）；函数指针免 PLT——DLL 中间件用函数指针而非导出的原因，值得验证。
- 最终约束：**分派决策在 LLVM 层的真实生成形态已被验证；动态链接仅在开放世界需要时启用，且其不透明边界的代价被显式接受并测量**，而不是默认"动态链接是免费的"。

### 6.6 维度 6 · 扩展形态 — 运行期动态编译 + 加载（evcxr 参考，非照搬）

- 初次作答：新增中间件的默认形态 = 编译期（源码/宏），运行期扩展是显式选择且锁定分派模型。
- 纠正 1：**"并非，默认形态就是运行期吧。。。类似 evcxr"**。
- evcxr 机制（`HOW_IT_WORKS.md`，参考 deepwiki 高层概览）：
  1. **解析分类**：`syn` 把输入切成语句/项，识别类型。
  2. **代码生成**：语句/表达式包进唯一命名函数，注入变量保存/恢复与 panic 处理。
  3. **运行时构建**：写成 crate → `cargo` 编译 → **shared object（.so）**。
  4. **加载调用**：`dlopen` 打开 .so → 查函数符号 → 调用（`libloading`）。
  5. **变量持久化**：`HashMap<String, Box<dyn Any + 'static>>`；类型靠 rustc 编译错误反推（先存 String，错误暴露真实类型后重编）。
  6. **子进程隔离**：所有代码跑在子进程，stdin/stdout 发命令（加载 .so + 运行其中函数）；**segfault 可重启**。
  7. **编译缓存**：按"编译命令 + 参数"哈希，命中复用，LRU 清理。
  8. **库永不卸载**（`mem::forget`）：防 TLS destructor 在卸载时 segfault；热替换 = 换 .so + 重载符号（Wild linker 为作者点名的未来方向）。
- 纠正 2（关键）：第二次作答把 evcxr 照搬成默认，宣称"**D2 分派被强制为开放世界、闭合世界出局**"——这推翻了 D1~D5。用户纠正：**"不行，不能推翻我们原来的 5 条"**。
- 最终定稿（参考非照搬）：
  - **运行期加载的产物天然落槽，不强制开放化**：evcxr 编译出的就是 `extern "C"` 函数 → thin fn 指针（无状态，8B）；有状态的走工厂返回 `Box<dyn>`。它们进链的 fn-ptr / dyn 槽位，**不是把全部中间件拖进开放世界**。D2 保留。
  - **闭合世界没出局**：编译期已知中间件（引导集、观测中间件）仍可入 enum、可 cfg 剥离；运行期加载的是新增的、罕见的（D 次线性）那一批。异构共存：
    ```rust
    enum Node {
        Builtin(Auth),            // 编译期已知 → 零成本落槽
        Plugin(fn(&mut Ctx)),     // 运行期加载、无状态 → thin 指针
        Dyn(Box<dyn Middleware>), // 运行期加载、有状态 → fat 指针
    }
    ```
  - **D5 反而更成立**：evcxr 的编译 → dlopen → 符号解析 → 重定位正是 D5 要研究的真实动态链接，成了研究的载体。
  - **D1/D3/D4 原样**：调用点零成本；加载 = 构造新快照替换；加载是加法（编译延迟 + dlopen，不进稳态热路径），空链透明。
  - **拿 vs 扔**：拿 evcxr 管线（syn→crate→.so→dlopen→符号→缓存→永不卸载）；**扔 REPL 壳**（子进程模型——中间件应在宿主进程内加载，隔离需求不同；变量持久化——REPL 交互特性，中间件状态自管；逐求值重编译——中间件是"一次编译长期驻留"，重点是单中间件编译延迟；输出捕获转发）。
- 最终约束：**运行期动态编译 + 加载（evcxr）是中间件集合增长的默认形态，但它是 D1~D5 的超集——往链里加节点，节点仍按 D2 落槽，不改变任何已有承诺。**

### 6.7 维度 7 · 安全与定位 — 不安全收口边界层 + 信任模型明确

- **ABI 契约显式化**：跨边界接口 = `extern "C"` 函数指针 + ABI 兼容的 Ctx 结构；导出版本符号，加载时校验，杜绝 Rust 版本/依赖不匹配的 UB。
- **unsafe 收口**：dlopen、符号解析、指针转换全部压进一层薄 boundary，对外只暴露安全 API。
- **panic 不跨界**：`extern "C"` 边界必须 `catch_unwind` 兜住——panic 跨 FFI 是 UB。
- **Send/Sync 显式契约**：加载的中间件状态必须 Send+Sync（链被多线程共享），加载先于共享或走同步。
- **热替换的卸载问题**：按 evcxr 经验**永不直接 unload**（TLS destructor 会 segfault）→ 热替换 = 加载新版 .so，旧版延迟释放/泄漏。
- **信任模型**：进程内 dlopen = 执行任意原生代码。可信生产插件走 in-process；**只有不可信插件才需要** evcxr 式子进程沙箱——可选增强，非默认。
- **定位 = 生态交集**：tower 有生产 Service 语义（短路/上下文/错误/async）但无运行期热重载；evcxr 有运行期编译加载但无生产中间件语义。**本系统 = 生产 Service 形状 × evcxr 机制 = 生产级可热重载中间件层**，生态空白。

### 6.8 维度 8 · 迁移工具链（最后做）— 渐进、按域、语义等价

- 用户补充的维度，明确**最后才做**（前 7 维定稿后，工具才能按最终 API 形状写）。
- **静态分析先行**：识别两类目标——候选核心（应被包裹的处理器/服务方法）与**内联横切逻辑**（散落的日志/鉴权/超时/重试/计时，抽成中间件的原料）。
- **AST 改写（codemod）**：包装候选核心为 `DynamicProc::new(core)` + 链配置；把内联横切代码提取成中间件定义；调用点同步改写。Rust 侧工具：`comby` / syn 系 codemod（参考 `cargo fix` / `rustfix` 机制）。
- **按域渐进采纳**：一次只改一个领域，其余保持编译——对应"中间件限定在门面层"的结论，**加性安全**，任何一步可停可回滚。
- **行为等价验证**：改造前后对同一输入的输出一致——中间件链不改变核心语义，只附加上下文/观测。这是迁移正确性的判据。
- **编译反馈闭环**：目标"快速产出可编译骨架"，编译错误驱动下一轮修正；主要痛点是类型/生命周期/Send-Sync 在包装后的暴露。

### 6.9 维度间关系（依赖链）

- **D6 前置约束 D2**：运行期扩展要求分派支持开放世界（fn-ptr/dyn 槽位），闭合 enum 无法被运行期扩展——若不需要运行期扩展才有资格保持纯闭合。
- **D3 锁死 D4**：空链透明（含 Debug）要求读路径无锁无分配 → RCU 快照而非 Mutex。
- **D5 是 D6 的载体**：evcxr 的编译→dlopen→重定位正是 D5 要研究的真实动态链接。
- **D1~D5 是纸面承诺，D4 是验收**：理论代价必须等于实测代价（基准 + 反汇编）。
- **D8 依赖前 7 维**：迁移工具按最终 API 形状写，最后做。
