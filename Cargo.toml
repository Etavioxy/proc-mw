[package]
name = "proc-mw"
version = "0.1.0"
edition = "2021"
description = "生产 Service 语义 × 运行期热重载的中间件层 — 八维约束在同一项目中持续验证"

[lib]
name = "proc_mw"
path = "src/lib.rs"

[[example]]
name = "d4_bench"
path = "examples/d4_bench.rs"

[dev-dependencies]
